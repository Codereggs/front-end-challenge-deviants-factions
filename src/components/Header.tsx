import { GridItem } from "@chakra-ui/react";
import React from "react";

const Header: React.FC = () => {
  return <GridItem gridArea={["1 / 1 / 2 / 7"]} bg="black"></GridItem>;
};

export default Header;
